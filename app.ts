// Function to get
function getSubstring(sentence: string, start: number, end: number): string {
    return sentence.substring(start, end);
}

console.log(getSubstring("Learning Typescript is different than Javascript", 9, 19));